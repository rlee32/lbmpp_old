# Use hashtag for comments.

# Grid definition.
coarse_cells 128 128 # cells in x, y direction.

# Physical parameters; these are used to compute the viscosity.
M 0.006 # Characteristic Mach number. This value is also used for BC.
L 128 # Characteristic Length. Must ensure consistency with cell count. 
Re 100 # Reynolds number

# Numerical parameters.
nucf 0 # multiple of viscosity to serve as a buffer viscosity.
# Relaxation time model:
# 1: Single-relaxation time.
# 2: Two-relaxation time.
# 3: Multiple-relaxation time.
relax_model 3 

# Solution and run time control.
timesteps 250000 # maximum number of timesteps to run.
refinement 0 # solution-adaptive refinement: 1 = true, 0 = false

# Initial macro-scale variable values.
rho0 1
u0 0
v0 0

# boundary conditions
# m: moving wall (value is M)
# w: stationary wall
# i: inlet (value is M)
# o: zero-gradient distribution outlet
bottom w
right w
top m
left w


